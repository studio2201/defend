# Defend

Retro-neon space shooter game.

## Quick Start

### Self-Hosting (Docker)
Pull and run the official Docker container:
```bash
docker run -d -p 4504:4504 -v /path/to/appdata:/app/data ghcr.io/dachoras/defend:latest
```
